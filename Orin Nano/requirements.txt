# Echo-Vision: Pi 5 face detection + identification module
# Install with: pip install -r requirements.txt --break-system-packages
#
# Note (Pi 5 / ARM64): torch/torchvision wheels for ARM can be slow to build
# from source. If plain pip install is too slow, use the piwheels index:
#   pip install -r requirements.txt --break-system-packages \
#       --extra-index-url https://www.piwheels.org/simple

requests>=2.28.0
numpy>=1.23.0
pyaudio>=0.2.13
faster-whisper>=1.0.0
gTTS>=2.5.1
opencv-python>=4.8.0

# CSI ribbon-cable camera support (Raspberry Pi OS Bookworm / libcamera).
# Usually already installed system-wide via: sudo apt install -y python3-picamera2
# If you're using a venv, create it with --system-site-packages so it can
# see the system picamera2 install (it depends on libcamera which isn't
# pip-installable in the normal way).
# picamera2
